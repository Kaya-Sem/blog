import pluginDate from "@11ty/eleventy-plugin-date";

export default function (eleventyConfig) {
	eleventyConfig.addPlugin(pluginDate);

	// Add other custom configurations here if needed

	return {
		// Other configuration options
	};
};
